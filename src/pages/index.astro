---
import { getEntry } from "astro:content";
import Hero from "../components/Home/Hero.astro";
import MainLayout from "../layouts/MainLayout.astro";

const homepage = await getEntry("pages", "homepage");
const { data }: any = homepage;
---

<html lang="en">
  <MainLayout>
    <Hero data={data} />
  </MainLayout>
</html>
